{
  "private": true,
  "workspaces": [
    "bad-app",
    "note-app"
  ],
  "scripts": {
    "build:bad-app": "yarn workspace bad-app build",
    "build:note-app": "yarn workspace node-app build"
  }
}